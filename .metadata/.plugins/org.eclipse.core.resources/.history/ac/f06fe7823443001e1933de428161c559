package a32Type_Inference;

public class aSample {
	
	/*
	 * Type Inference is a concept in which the compiler infers the type of variable using the value
	   provided.
	   1. This Type Inference is restricted to local variables.
	   2. This feature is introduced in java 10 version.
	 */
	
	/*
	 var keyword restrictions :
	 * var can be used in the local variables.
	 * var can't be used for only declaration purpose, insted it should be used along with initializastion.
	 * when used with arrays, should not use the []
	 * var can't be used in In-line array initialisation
	 */

}
